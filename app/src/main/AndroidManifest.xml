<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.birger.mainmenuactivity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListViewActivity" />
        <activity android:name=".GalleryActivity" />
        <activity android:name=".AddNewPerson" />
        <activity android:name=".ImgNameDetail" />
        <activity android:name=".LearningModeActivity" />
        <activity android:name=".Result" />
        <activity android:name=".ImageAdapter" />
        <activity android:name=".FirstRunAdd"></activity>
    </application>

</manifest>